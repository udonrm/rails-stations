<div class="container text-center mt-5">
    <div class="row justify-content-center">
        <div class = "col-4">
        <%= flash[:alert]%>
        <h1>ご予約ページ</h1>
        <%= form_with model: @reservation, url: reservations_path do |f| %>
            <% if !@movie.nil? %>
                <%= f.label :movie_id, "選択中の映画", class: "form-label" %>
                <%= f.text_field :movie_id, value: @movie.name, class: "form-control text-center", readonly: true%>
                <%= f.hidden_field :movie_id, value: @movie.id %>
            <% end %>

            <%= f.label :date, "選択中の日時", class: "form-label" %>
            <%= f.text_field :date, value: @date, class: "form-control text-center", readonly: true%>

            <%= f.label :sheet, "選択中のsheet_id", class: "form-label" %>
            <%= f.text_field :sheet_id, value: @sheet, class: "form-control text-center", readonly: true%>

            <%= f.label :schedule, "選択中のschedule_id", class: "form-label" %>
            <%= f.text_field :schedule_id, value: @schedule, class: "form-control text-center", readonly: true%>

            <%= f.label :email, "メールアドレス", class: "form-label mt-3" %>
            <%= f.text_field :email, class: "form-control text-center" %>

            <%= f.label :name, "名前", class: "form-label" %>
            <%= f.text_field :name, class: "form-control text-center" %>

            <%= f.submit "確定", class: "mt-3" %>
        <% end %>
        </div>
    </div>
</div>